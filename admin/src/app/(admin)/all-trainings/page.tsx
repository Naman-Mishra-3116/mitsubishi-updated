import ViewAllTrainings from "@/components/views/fragments/ViewAllTrainings";
import React, { memo } from "react";

const page: React.FC = () => {
  return <ViewAllTrainings />;
};

export default memo(page);
