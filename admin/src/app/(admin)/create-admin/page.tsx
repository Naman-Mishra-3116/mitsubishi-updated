import CreateAdminContainer from "@/components/admin-details/fragments/CreateAdminContainer";
import React, { memo } from "react";

const page: React.FC = () => {
  return <CreateAdminContainer />;
};

export default memo(page);
