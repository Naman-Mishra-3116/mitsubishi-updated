import YearInfoContainer from "@/components/yearly-info/fragments/YearInfoContainer";
import React, { memo } from "react";

const page: React.FC = () => {
  return <YearInfoContainer />;
};

export default memo(page);
