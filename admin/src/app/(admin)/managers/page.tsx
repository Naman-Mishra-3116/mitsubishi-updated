import ViewAllManagers from "@/components/views/fragments/ViewAllManagers";
import React, { memo } from "react";

const page: React.FC = () => {
  return <ViewAllManagers />;
};

export default memo(page);
