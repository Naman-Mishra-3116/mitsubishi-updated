import VerifyCertificate from "@/components/auth/fragments/VerifyCertificate";
import React, { memo } from "react";

const page: React.FC = () => {
  return <VerifyCertificate />;
};

export default memo(page);
