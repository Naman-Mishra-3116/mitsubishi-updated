import LoginContainer from "@/components/auth/fragments/LoginContainer";
import React, { memo } from "react";

const page: React.FC = () => {
  return <LoginContainer />;
};

export default memo(page);
