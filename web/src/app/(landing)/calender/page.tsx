"use client";
import CalenderContainer from "@/components/calender/fragments/CalenderContainer";
import React, { memo } from "react";

const page: React.FC = () => {
  return <CalenderContainer />;
};

export default memo(page);
