import AllATCContainer from "@/components/centers/fragments/AllATCContainer";
import React, { memo } from "react";

const page: React.FC = () => {
  return <AllATCContainer />;
};

export default memo(page);
