import SpecificTraining from "@/components/training/fragments/SpecificTraining";
import React, { memo } from "react";

const page: React.FC = () => {
  return <SpecificTraining />;
};

export default memo(page);
